services:
  # ------------------- KAFKA (Redpanda) -------------------
  kafka:
    image: redpandadata/redpanda:v23.3.18
    container_name: kafka
    command:
      - redpanda
      - start
      - --overprovisioned
      - --smp 1
      - --memory 1G
      - --reserve-memory 0M
      - --node-id 0
      - --check=false
      - --kafka-addr INTERNAL://0.0.0.0:9092,EXTERNAL://0.0.0.0:19092
      - --advertise-kafka-addr INTERNAL://kafka:9092,EXTERNAL://localhost:19092
    ports:
      - "19092:19092"

  # ------------------- MONGODB -------------------
  mongodb:
    image: mongo:7
    container_name: mongodb
    ports:
      - "27018:27017"

  # ------------------- TRACKING SERVICE -------------------
  tracking-service:
    build:
      context: ./tracking-service
    container_name: tracking-service
    depends_on:
      - kafka
      - mongodb
    environment:
      APP_NAME: tracking-service
      API_HOST: 0.0.0.0
      API_PORT: 8080
      KAFKA_BOOTSTRAP_SERVERS: kafka:9092
      KAFKA_TOPIC: drone-events
      KAFKA_GROUP_ID: tracking-service
      MONGO_URI: mongodb://mongodb:27017
      MONGO_DB_NAME: tracking
      MONGO_COLLECTION_NAME: tracking_states
      DELIVERY_SERVICE_BASE_URL: http://host.docker.internal:8080
    ports:
      - "8083:8080"
