server:
  port: ${SERVER_PORT:8085}

services:
  delivery:
    base-url: ${DELIVERY_SERVICE_BASE_URL:http://localhost:8080/deliveries}
  user:
    base-url: ${USER_SERVICE_BASE_URL:http://localhost:8081/users}
  payment:
    base-url: ${PAYMENT_SERVICE_BASE_URL:http://localhost:8083/payments}
  dronefleet:
    base-url: ${DRONEFLEET_SERVICE_BASE_URL:http://localhost:8082/drones}
